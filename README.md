# Big Easy Painters — homepage redesign preview

Static preview of the redesigned homepage. **Draft, not the live site.**

- `index.html` — the page
- `assets/` — every image, hosted locally (nothing is pulled from bigeasypainters.com)
- `banner-paint-blue.mp4` + poster — the hero banner video

`<meta name="robots" content="noindex,nofollow">` is set deliberately so this preview
cannot compete with the live site in search. Remove it only when the page goes live on the
real domain.
